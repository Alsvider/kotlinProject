<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--默认分割线-->
    <style name="CommonWidget"></style>
    <style name="CommonWidget.Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_0.5</item>
        <item name="android:background">@color/actionbar_divider</item>
    </style>

    <!-- Toolbar返回按键颜色 -->
    <style name="AppTheme.ToolBar.BackKey" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--<item name="colorControlNormal">@color/btn_back</item>-->
        <item name="android:homeAsUpIndicator">@mipmap/ic_back</item>
    </style>
</resources>